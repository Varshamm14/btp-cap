CREATE TABLE CATEGORY (
    ID INT NOT NULL PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    Name NVARCHAR(100) NOT NULL, -- Medium length field for Name
    Description NVARCHAR(500),   -- Optional Description
    Status NVARCHAR(50) NOT NULL, -- Status (Available/Out of Stock)
   PRIMARY KEY ("id")) UNLOAD PRIORITY 5 AUTO MERGE
